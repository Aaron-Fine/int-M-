{
  "name": "mandelbrot-interiority",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An evidence-aware explorer for the interior dynamics of the Mandelbrot set.",
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=22.13.0 <25",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && npm run build:assets",
    "build:assets": "vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit -p tsconfig.app.json && tsc --noEmit -p tsconfig.worker.json && tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.e2e.json",
    "catalog:check": "python3 tools/generate_catalog.py --check catalog/components.v1.json",
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "vitest run --passWithNoTests",
    "test:unit:watch": "vitest",
    "test:browser": "playwright test --pass-with-no-tests",
    "test:browser:headed": "playwright test --headed",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run catalog:check && npm run test:unit && npm run build:assets"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.0",
    "@types/node": "26.1.2",
    "eslint": "10.8.0",
    "globals": "17.8.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
